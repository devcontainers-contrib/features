{
  "image": "mcr.microsoft.com/devcontainers/universal",
  "features": {
    // 🛑 Uncomment this before merging!
    // "ghcr.io/devcontainers-contrib/features/bash-utils": {}
  },
  "postCreateCommand": "npm install -g @devcontainers/cli prettier",
  "customizations": {
    "vscode": {
      "extensions": [
        // 🛑 Remove this Bash extension pack before merging. It's provided by
        // the bash-utils feature.
        "lizebang.bash-extension-pack",
        "xdebug.php-pack",
        "esbenp.prettier-vscode"
      ]
    }
  }
}

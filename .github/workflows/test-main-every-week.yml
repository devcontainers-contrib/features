name: Test main every week
on:
  workflow_dispatch:
  schedule:
    # https://crontab.guru/once-a-week
    - cron: 0 0 * * 0
jobs:
  test-main-every-week:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: tools/test.sh

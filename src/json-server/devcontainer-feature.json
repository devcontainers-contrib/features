{
    "id": "json-server",
    "version": "1.0.4",
    "name": "JSON Server (via npm)",
    "documentationURL": "http://github.com/devcontainers-extra/features/tree/main/src/json-server",
    "description": "Get a full fake REST API with zero coding in less than 30 seconds.",
    "options": {
        "version": {
            "default": "latest",
            "description": "Select the version to install.",
            "proposals": [
                "latest"
            ],
            "type": "string"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers-extra/features/npm-package",
        "ghcr.io/devcontainers/features/node"
    ]
}
